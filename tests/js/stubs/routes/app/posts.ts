/**
 * Stub for the Wayfinder-generated route helper. `resources/js/routes` is built
 * by artisan and gitignored, so unit tests resolve `@/routes/*` here instead of
 * requiring a generated tree to be present.
 */
export const edit = (id: string) => ({ url: `/posts/${id}/edit` });
